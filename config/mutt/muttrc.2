unalternates *
alternates '@evanchen\.cc' '^evanchen@mit\.edu$'

set folder=~/Mail/records/
set header_cache=~/.cache/mutt/records

set smtp_url = $my_smtp_url2
set smtp_pass = $my_key2
set from = $my_user2
unmy_hdr Bcc

set imap_user = $my_user2
set imap_pass = $my_key2
set spoolfile = +Inbox
set postponed = +Drafts

color indicator  brightwhite red
color status     brightwhite red

color sidebar_divider red white
# vim: ft=neomuttrc
